---
title: REPLACE_TITLE
description: REPLACE_ONE_LINE_SUMMARY
diataxis: how-to
---

# REPLACE_TITLE

**Diátaxis:** how-to — solve a specific problem.  
**Goal:** REPLACE_GOAL_IN_ONE_SENTENCE

## Prerequisites

- REPLACE_PREREQ — see [REPLACE_LINK](REPLACE_PATH)

## REPLACE_TASK_NAME

Short intro before the procedure.

1. REPLACE_STEP — run from the repo root:

```bash
REPLACE_COMMAND
```

`YOUR_PLACEHOLDER` — how to obtain it (link to auth or deploy guide).

Expected result:

```text
REPLACE_OUTPUT
```

2. REPLACE_NEXT_STEP

## Verify

How the reader knows it worked.

## Troubleshooting

| Symptom | Fix |
|---------|-----|
| REPLACE_ERROR | REPLACE_FIX |

## Related

- [REPLACE_DOC](REPLACE_PATH) — what the reader gets from it.
