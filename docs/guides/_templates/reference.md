---
title: REPLACE_TITLE
description: REPLACE_ONE_LINE_SUMMARY
diataxis: reference
---

# REPLACE_TITLE

**Diátaxis:** reference — facts for lookup, not a tutorial.

## REPLACE_SECTION

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| REPLACE | REPLACE | Yes/No | REPLACE |

## Examples

### REPLACE_EXAMPLE_NAME

```bash
curl -sS -X GET "${YOUR_API_BASE_URL}/v1/REPLACE" \
  -H "Authorization: Bearer ${YOUR_JWT}"
```

`YOUR_API_BASE_URL` — stack output `ApiUrl` from [Deployment Guide](../DEPLOYMENT_GUIDE.md).  
`YOUR_JWT` — from `bgagent login` (see [Authentication](../USER_GUIDE.md#authentication)).

## See also

- [REPLACE_HOW_TO](../REPLACE.md) — procedural guide for the same feature.
