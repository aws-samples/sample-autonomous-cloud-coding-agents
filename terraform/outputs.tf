# Outputs for the ABCA root module.
#
# Intentionally empty. This PR provisions nothing, so there is nothing to
# export. Later PRs in the stack populate this file as they add resources —
# expect the REST API invoke URL, the AgentCore Runtime ARN, the Memory id, and
# the Cognito/authorizer details, mirroring the CfnOutputs the CDK stack emits.
